= form_for [@product, @product.auction_product, Bid.new] do |f|
  %p
    %strong
      Current Bid:
      = @product.auction_product.current_bid

  %p
    = f.text_field :value
    = f.button class: 'btn btn-default'
